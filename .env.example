# Puerto
PORT=3000

# CSV público (pestaña correcta) debe terminar en output=csv
SHEETS_CSV_URL=

# Debug opcional
DEBUG_YT=true

# Persistencia de contexto (opcional)
REDIS_URL=
SESSION_TTL_SECONDS=86400

# PROMPT
# Si quieres mover el prompt fuera, ajusta estas:
PROMPT_FILE=./PROMPT.json
PROMPT_AUTO_RELOAD=true
PROMPT_URL=  # opcional: si defines una URL http(s) a un JSON, el backend la traerá

