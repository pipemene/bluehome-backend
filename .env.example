# Puerto del servidor
PORT=3000

# URL pública de Google Sheets exportada como CSV (NO tocar si ya funciona)
SHEETS_CSV_URL=<tu_url_de_google_sheets_con_output=csv>

# Debug de YouTube (solo logs de consola)
DEBUG_YT=true

# Persistencia del contexto (opcional)
REDIS_URL=
SESSION_TTL_SECONDS=86400

# Prompt
PROMPT_FILE=./PROMPT.json
PROMPT_AUTO_RELOAD=true
# Si quieres cargar el prompt desde Internet:
# PROMPT_URL=https://tuservidor.com/prompt.json
